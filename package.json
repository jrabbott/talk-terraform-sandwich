{
  "name": "terraform-sandwich",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Terraform Sandwich — multi-stage Terraform pattern talk deck (Vite + reveal.js)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.3.0",
    "reveal.js": "^6.0.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
