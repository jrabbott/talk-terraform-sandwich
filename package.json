{
  "name": "slides-as-code",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable Vite + reveal.js starter template for talk slides",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.3.0",
    "reveal.js": "^6.0.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
